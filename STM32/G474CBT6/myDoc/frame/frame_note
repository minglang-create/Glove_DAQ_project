- DAQ（手套）框架：
- RV1126B软件框架：采用单进程多线程模型，采集类线程设置 SCHED_FIFO 实时调度策略，分配高优先级！
    - 自检函数，记录传感器状态，状态机写法：
        - 1.首先自检SD卡和USB HOST对应的USB HUB上有多少USB设备，记录活跃情况。随后SPI读STM32，是否通信，定义一个通信测试包。STM32会提前转载好通信测试包。
        - 2.主从机分配阶段：一直检测按键是否拉低以及不断读取SPI，如果按键先拉低，则SPI发送”我是主机“，等待从机回传“收到”，完成本次握手。如果SPI先读到”我是从机“，回传“收到”，完成本次握手，如果等待2S没有握手成功（同时一起按下会冲突，握手机制防止同时一起按下，如果出现这个情况，等待几s重新按其中一个即可），则退回到等待分配阶段。
        - 3.匹配成功后，主机STM32拉低XVS 2s后（给从机摄像头启动时间），主机STM32占用XVS总线开始发出60Hz信号，给主从机自检摄像头双摄同步。
        - 4.STM32与RV1126B之间的SPI通信包装成类似USB事务包的形式。比如，STM32在任何时间自检完成后装载一个自检包。rv1126B在读到过这个包便可以认为STM32自检完成，如果STM32在前几步没有发送自检完成包，则该阶段等待STM32回传自检包，SPI读每100ms执行一次，直到SPI读到要求的有效数据或超时，事务包汇总见TABLE-1。
        - 5.RV1126B完成摄像头自检后，向STM32写自检完成包，STM32停止发XVS，全机进入等待运行状态。
        - 6.落盘log自检信息，整个自检过程中任何一步失败即刻落盘log记录问题，由于本系统为联网，也没有RTC，考虑使用一个文件记录每次上电情况，每次上电首先给这个文件中的上电次数变量power_count++，log记录时间戳时，可以通过本机时间+power_count*天。依次来区分最新上电的日志。
    - 开始：通过UI交互工人知道手套是否正常，自检完成后，工人需要按下先前按的主机的按键，主机RV发送开采包通知STM32开始激活XVS，摄像头开始工作，STM32开始采集，并回传，一般前几帧视频不稳定，可以丢弃。STM32会通过PA1告诉RV1126B的GPIO0_A4_D，高电平表达数据准备就绪，同时PA1还可用于视频与数据的同步对齐。
    - 运行架构：额外建立3个数据采集线程（SPI从机、主摄像机、副摄像机）。主程序负责数据解包，任务安排，数据落盘等。
    - 暂停：按下主机按键，RV发送停采包，XVS信号停止，数据采集停止。
    - 结束：暂停或者运行阶段下，长按主机按键即可结束。
    
- MCU软件框架：目标兼容手套和手，将STM32整个系统视为一个传感器，能通过SPI事务包向其发送任务，以及能读取整手传感器信息，其GPIO开漏输出的信号表征了其内部传感器一次同步采集，使用STM32的PA1高电平表达STM32数据准备就绪。RV1126B的GPIO0_A4_D与STM32 PA1相连。
    - 自检：首先转载SPI通信测试包，帮助检测与RV的通信。5路CAN总线挂载检测、IMU方案I2C自检。自检完给SPI装载自检包。
    - 主从机分配阶段：读XVS的电平与SPI接受的信息，当XVS先来有效位则认为是从机，装载”是从机“包给RV；如果RV先发来”是主机“包，则认为是主机，STM32拉低XVS 2s后，主机STM32占用XVS总线开始发出60Hz信号，直到收到主机的自检完成包，停止XVS，进入等待运行状态。
    - 运行：自检完成后进入就绪态，从机一旦收到XVS的上升沿（TIM CH捕获中断），自动记录当前XVS心跳次数（这个过程主机则在收到RV发来的开采包后，开始自行发出信号，并计数），然后开始CAN广播，让5路CAN采集数据，再I2C读取IMU数据+解算，5路传感器采集完毕自动回传，检测到所有数据回采完成，立刻转载数据到SPI，最后拉高PA1告诉RV可以拿走数据。
    - 暂停与结束：主机STM32收到停采包，暂停XVS。


    